apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-aws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-aws
  template:
    metadata:
      labels:
        app: kargo-aws
    spec:
      containers:
      - image: public.ecr.aws/nginx/nginx:1.30.0
        name: nginx
        volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: content
          readOnly: true
      volumes:
      - configMap:
          name: kargo-aws-content
        name: content
